# themes

The same deck under six themes, taken from keycap colorways. Swap the
`todan/themes/*.css` import in `src/index.tsx` to compare them.

`olivia` · `noel` · `nine009` · `botanical` · `dolch` · `laser`

```
vp dev
```
