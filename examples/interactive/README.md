# interactive

A live tally, an rAF animation driven by `mounted()` / `unmounted()`, and a text field —
the things a Markdown deck cannot do.

The text field also shows the one real cost of rebuilding on `setState`, and how
`updated()` pays it back.

```
vp dev
```
