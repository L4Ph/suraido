# create-todan

```
npm create todan@latest my-deck
```

[todan](../todan) のスライドプロジェクトを作る。

生成物には `AGENTS.md` が入っていて、todan の作法（`class` を使う・`deck()` に渡す配列は
ラップしない・`setState` は DOM を作り直す・スタイルは `@layer` の外から上書きする、など）を
コーディングエージェントに伝える。
